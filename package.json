{
  "name": "create-next-example-app",
  "scripts": {
    "dev": "yarn add git@github.com:alansutherland/heroku-test.git && next",
    "build": "next build",
    "start": "yarn build && next start -p $PORT"
  },
  "dependencies": {
    "next": "^6.0.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2"
  }
}
